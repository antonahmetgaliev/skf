// Local runtime config. Copy to public/env.js (gitignored) and fill in as needed.
// In production server.js generates this file from environment variables instead.
// Public values only — this is served to every visitor.
window.SKF_ENV = {
  // GA4 measurement ID, e.g. 'G-XXXXXXXXXX'. Empty disables analytics, which is what you want
  // locally unless you are specifically testing the banner — a real ID here reports to real GA.
  GA_MEASUREMENT_ID: ''
};
