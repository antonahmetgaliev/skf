[pytest]
asyncio_mode = auto
testpaths = tests
# Run with: backend/.venv-test/Scripts/python -m pytest
# (.venv-test uses Python 3.13 which has pre-built greenlet / aiosqlite wheels;
#  the production .venv uses Python 3.14 which lacks the greenlet C extension.)
